import $ from "jquery";
import "modules/arctext";

export default {
    init() {
        //$('[data-arc]').each(function() {
        //    var arc = parseInt($(this).data('arc'));
        //    $(this).arctext({radius: arc});
        //})
    }
}